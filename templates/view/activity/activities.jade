.xepan-grid.main-box.clearfix.atk-table.atk-table-zebra.atk-table-bordered(id="{$_name}" class="{$class}")
    h2.text-center {$grid_title}
    {Pannel}
    header.main-box-header.clearfix.atk-actions(class=" {$class} ")
        div.filter-block( style="100%")
            .pull-right {$grid_buttons} 
            .form-group.pull-right.xepan-filter-form
                span(class="form-control pull-left")|{$quick_search}
                    i(class="fa fa-search search-icon")
    {/Pannel}
    .main-box-body.clearfix.table-responsive
        table.table.user-list.table-hover(style="width:100%")
            {header}
            thead
                {cols}{col}
                th Activity Message
                th Created By
                th Related Document Detail
                th Related Person
                th Created At
                {/}{/}
            {/}
            {$subheader}
            //- {contact}Vijay mali{/} &nbsp; {related_contact} Avengers Initiative{/}
            tbody
                {rows}{row}
                tr(class="{$odd_even} {$score_class}" data-id="{$id}" data-contacttype="{$contact_type}")
                    {cols}
                    td(style="font-size: 0.87em; font-weight: 400;")
                        |{$activity}&nbsp;
                    td
                        span(style= "max-width:600px;"){$contact}
                    td 
                        span(style= "cursor:pointer; cursor: hand; max-width:600px;" onclick="javascript:$.univ().frameURL('Document Detail','?page={$document_url}');"){$related_document_id}
                     td
                        span(style= "cursor:pointer; cursor: hand; max-width:600px;" onclick="javascript:$.univ().frameURL('Person Detail','?page={$contact_url}');"){$related_contact}
                    td
                     | {$created_at}
                    {/}
                {/}{/}
                {not_found}
                div.row
                    .col-md-12.text-center.alert.alert-warning(role="alert")
                        span.icon-attention.fa.fa-exclamation-triangle  &nbsp;{$not_found_message}
                {/}

            {totals}
            tfoot
                tr(class="totals")
                    {cols}{col}
                    <td {$tdparam}>
                    {$content}
                    </td>
                    {/}{/}
            {/}
        {$Paginator}
